import { AttachcontentPipe } from './attachcontent.pipe';

describe('AttachcontentPipe', () => {
  it('create an instance', () => {
    const pipe = new AttachcontentPipe();
    expect(pipe).toBeTruthy();
  });
});
