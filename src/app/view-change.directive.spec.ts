import { ViewChangeDirective } from './view-change.directive';

describe('ViewChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewChangeDirective();
    expect(directive).toBeTruthy();
  });
});
