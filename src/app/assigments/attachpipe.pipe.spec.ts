import { AttachpipePipe } from './attachpipe.pipe';

describe('AttachpipePipe', () => {
  it('create an instance', () => {
    const pipe = new AttachpipePipe();
    expect(pipe).toBeTruthy();
  });
});
